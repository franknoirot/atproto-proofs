node_modules/
lean/.lake/
lean/build/

# Generated by the demo: the offline atproto repos, and the scratch Lean packages
# the checker builds each obligation in. Both are reproducible from seed/.
.data/
.work/

# Slidev build output and caches. `presentation/data.json` IS committed, so the
# deck opens from a clean clone without re-running the pipeline.
presentation/dist/
presentation/deck.pdf
.slidev/

*.log
.DS_Store
